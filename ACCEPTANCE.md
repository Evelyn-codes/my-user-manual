# V2 验收记录（2026-09-19）


## 内容和计分

- 从新版 PRD 独立提取 72 组原始分值，与项目数据逐项比较一致。
- 18 张结果全部存在，唯一结果键、代码、三维描述和角色映射一致；`A1-B1-C1` 为勿扰人 DND。
- 10,000 组固定种子的答案验证总分、确定性和数据条公式；覆盖全部 18 种结果。
- A/B 的 -6/-2/-1/0/+1/+2/+6 边界和正负 C 分档通过。
- 专门覆盖 C=0、Q18 分别为 A/B/C/D，归入 C1/C2 正确。
- V2 的 72 组选项分值全部替换；Q18 正常计分，C=0 时 A/B→C1，C/D→C2。

## 用户进度

- 到第六题未选择时关闭页面，在同一浏览器的新页面进入仍是第六题，前五题原答案恢复。
- 第六题选择后未切题刷新，仍停第六题且选中原答案。
- 返回第五题修改后刷新，仍停第五题并保留修改。
- 回首页后退出，再进入恢复之前题号。
- 最后一题提交后完成状态立即写入；关闭计算过渡中的页面，重开直接展示真实结果。
- 完成后刷新或通过带朋友参数的链接重开，仍展示自己的结果。
- 取消重测保留答案；确认重测替换旧轮次，答案清空且回第一题。
- 多页面同步新一轮与当前题号；旧页面历史导航不能复活旧答案。
- 存储结构损坏、非法选项、非法题号、版本不符均有校验；不能伪造“已完成”但缺题的记录。
- 模拟 quota 错误：继续答题不崩溃，明确提示无法持久化。

## 海报与移动端

- 375 / 390 / 430 / 462 CSS px 下，18 种真实结果及海报共 72 组检查，无横向溢出。
- 18 张 PNG 均为 1080px 宽，高度不少于 1440px，随正文长度变化。
- 独立使用 jsQR 解码全部 18 张导出图片的二维码，确认首页 origin、`from=poster` 与对应结果键准确，URL 无 hash 或答案参数。
- 角色依赖独立映射，结果名称和正文来自同一真实结果对象；固定示例不进入正式用户流程。
- 图鉴不显示本轮数据、不替换本轮结果；重开仍回自己的结果。
- 剪贴板权限失败可手动复制；海报生成错误可重试。
- 微信 UA 中显示真正的 PNG 和长按保存提示，不显示非微信下载入口；未冒充微信真机验证。
- 已视觉检查首页及多张真实导出海报；长 OS 使用均衡换行。

## 工程

- `npm test`：通过。
- `npm run test:ui`：开发环境通过。
- `npm run build`：TypeScript 与 Vite 生产构建通过。
- `TEST_URL=http://localhost:4173/ npm run test:ui`：生产构建浏览器复验通过；记录见 `test-results/checks.json`。
- 原素材总量约 13.5MB，项目副本优化至约 4.2MB；原始文件未改动。

## 仍需实际部署后验证

- 将 VITE_SITE_URL 设置为正式 HTTPS 首页并重新构建。
- 用 iOS / Android 的真实微信验证“扫一扫”和长按保存。当前测试为 Chromium 自动化及微信 UA 模拟，未声称已完成真实微信硬件验收。
- 无后台、无微信 JSSDK 签名服务，不提供一键朋友圈发布或自定义微信菜单分享卡。
